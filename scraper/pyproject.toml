[project]
name = "hyperscrape-tools"
version = "0.1.0"
description = "TUI, preview, master-db and Supabase upload tooling for the hyperscrape Go crawler"
requires-python = ">=3.12"
dependencies = [
    "rich>=13.7",
    "supabase>=2.4",
    "python-dotenv>=1.0",
    "zstandard>=0.22",
    "playwright>=1.45",
]

[tool.uv]
package = false
