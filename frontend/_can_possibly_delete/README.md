# `_can_possibly_delete` — frontend leftovers (not deleted)

Inspect later. Nothing here is imported by the running app.

| Path | What it was |
|------|-------------|
| `impeccable/` | Dump from the Impeccable AI design skill (HTML variants, critiques, design.json). Not part of the running app. |
| `impeccable-frontend-config/` | Was `frontend/.impeccable/` |
| `docs/` | Mock-era UI briefs (`browsing_page_guide.md`, original Home+Browse task) |
| `integration/` | Duplicate generated OpenAPI types; live types are `src/api/schema.d.ts` |
| `empty-dirs/` | Unused scaffold folders (`features`, `hooks`, `services`, `styles`, `types`) |

`src/api/client.ts` is the real API client — it stays in `src/api/`.

No extra empty placeholders were found at the frontend root this pass.
